import { Context } from '@actions/github/lib/context';
import { Octokit } from '@octokit/rest';
import { ApiHelper } from '@technote-space/github-action-helper';
import { Commit } from '../types';
export declare const getCurrentVersion: (helper: ApiHelper) => Promise<string>;
export declare const whatBump: (minorUpdateCommitTypes: string[], commits: Pick<Commit, "type" | "notes">[]) => "major" | "minor" | "patch";
export declare const getNextVersionLevel: (minorUpdateCommitTypes: string[], commits: Pick<Commit, "type" | "notes">[]) => number;
export declare const getNextVersion: (minorUpdateCommitTypes: string[], excludeMessages: string[], breakingChangeNotes: string[], helper: ApiHelper, octokit: Octokit, context: Context) => Promise<string>;
