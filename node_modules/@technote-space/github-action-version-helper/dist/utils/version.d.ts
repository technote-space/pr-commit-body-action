import type { Commit } from '../types';
import type { Context } from '@actions/github/lib/context';
import type { ApiHelper, Types } from '@technote-space/github-action-helper';
import type { Logger } from '@technote-space/github-action-log-helper';
import { VERSION_BUMP } from '../constant';
export declare const getCurrentVersion: (helper: ApiHelper) => Promise<string>;
export declare const whatBump: (minorUpdateCommitTypes: Array<string>, commits: Array<Pick<Commit, 'notes' | 'type'>>) => keyof typeof VERSION_BUMP;
export declare const getNextVersionLevel: (minorUpdateCommitTypes: Array<string>, commits: Array<Pick<Commit, 'notes' | 'type'>>) => number;
export declare const getNextVersion: (minorUpdateCommitTypes: Array<string>, excludeMessages: Array<string>, breakingChangeNotes: Array<string>, helper: ApiHelper, octokit: Types.Octokit, context: Context, logger?: Logger | undefined) => Promise<string>;
