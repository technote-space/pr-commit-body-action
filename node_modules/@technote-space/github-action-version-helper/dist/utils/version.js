"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getNextVersion = exports.getNextVersionLevel = exports.whatBump = exports.getCurrentVersion = void 0;
const github_action_helper_1 = require("@technote-space/github-action-helper");
const commit_1 = require("./commit");
const misc_1 = require("./misc");
const constant_1 = require("../constant");
const getCurrentVersion = async (helper) => helper.getLastTag();
exports.getCurrentVersion = getCurrentVersion;
const whatBump = (minorUpdateCommitTypes, commits) => {
    if (commits.filter(commit => commit.notes.length).length) {
        return 'major';
    }
    if (minorUpdateCommitTypes.length && commits.filter(commit => commit.type && minorUpdateCommitTypes.includes(commit.type)).length) {
        return 'minor';
    }
    return 'patch';
};
exports.whatBump = whatBump;
const getNextVersionLevel = (minorUpdateCommitTypes, commits) => constant_1.VERSION_BUMP[exports.whatBump(minorUpdateCommitTypes, commits)];
exports.getNextVersionLevel = getNextVersionLevel;
const getNextVersion = async (minorUpdateCommitTypes, excludeMessages, breakingChangeNotes, helper, octokit, context, logger) => {
    const commits = await commit_1.getCommits(minorUpdateCommitTypes, excludeMessages, breakingChangeNotes, octokit, context);
    misc_1.log(logger => logger.startProcess('Target commits:'), logger);
    misc_1.log(() => console.log(commits
        .filter(item => item.notes.length || item.type)
        .map(item => ({
        type: item.type,
        message: item.message,
        notes: item.notes,
        sha: item.sha,
    }))), logger);
    misc_1.log(logger => logger.endProcess(), logger);
    const current = await exports.getCurrentVersion(helper);
    misc_1.log(logger => logger.info('Current version: %s', current), logger);
    const next = github_action_helper_1.Utils.generateNewVersion(current, exports.getNextVersionLevel(minorUpdateCommitTypes, commits));
    misc_1.log(logger => logger.info('Next version: %s', next), logger);
    return next;
};
exports.getNextVersion = getNextVersion;
//# sourceMappingURL=version.js.map