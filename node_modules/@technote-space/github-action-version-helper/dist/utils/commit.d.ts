import { Context } from '@actions/github/lib/context';
import { Octokit } from '@octokit/rest';
import { Commit } from '../types';
export declare const getCommits: (types: string[], excludeMessages: string[], breakingChangeNotes: string[], octokit: Octokit, context: Context) => Promise<Commit[]>;
